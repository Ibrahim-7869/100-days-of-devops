services:
  frontend:
    image: nginx:latest
    ports:
      - "8080:80"
    networks:
      - app-network

  cache:
    image: redis:alpine
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
